import Typography from "@mui/material/Typography";

const DropZone = () => {
  return (
    <div id="dropZone" className="panel">
      <Typography variant="h5">Drag your file here.</Typography>
    </div>
  );
};

export default DropZone;
